package services;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.messaging.simp.SimpMessagingTemplate;
import org.springframework.stereotype.Service;

@Service
public class DeviceChangePublisher {

//    @Autowired
//    private SimpMessagingTemplate messagingTemplate;
//
//    // Publish to a topic when a device change occurs
//    public void notifyDeviceChange(String deviceId, String message) {
//        messagingTemplate.convertAndSend("/topic/deviceChanges/" + deviceId, message);
//    }
}

